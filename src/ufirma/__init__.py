"""ufirma — CLI for the whole loop: KSeF -> JPK_V7M -> e-Dokumenty gateway.

This package holds the CLI layer only (module :mod:`ufirma.cli`); the logic
lives in the library packages ``ksef`` (KSeF API 2.0 client) and ``jpk``
(building JPK_V7M and submitting it to the gateway).
"""
